# Testes
# Comando para conectar meu projeto local do GIT com o repositório Remoto no GitHub: git remote add origin urlDoRepositorioNoGitHub

# git clone urlDoProjeto (Clonar o projeto)
# git status (Verificar situação do repositório atual, arquivos que foram editados e etc)
# git add . (adicionar todas mudanças de uma vez ao repositório local.)
# git log (verificar histórico de alterações no sistema)
# git commit -m "Alguma Mensagem das alterações" (Commitar o código)